describe("Room Search Controller Spec", function() {

    it("Page changed, then go to other page", function() {
        expect(true).toBe(true);
    });

    it("check if the pagination works", function() {
        expect(1).toEqual(1);
    });

    it("check if the page num right", function() {
        expect(1).toEqual(1);
    });

    it("check if the params tag works", function() {
        expect(1).toEqual(1);
    });

    it("check if the params owner/roomname works", function() {
        expect(1).toEqual(1);
    });

    it("check if the params description works", function() {
        expect(1).toEqual(1);
    });

});