describe("Login Controller", function() {
    // var sayHelloNew;
    // var called = false;
    // beforeEach(function(done) {
    //     var oldHello = sayHello;
    //     sayHello = function(string) {
    //         called = true;
    //         done();
    //         return oldHello(string);
    //     }; 
    //     delaySayHello('Test');
    // });

    it("openLogin modals", function() {
        expect(true).toBe(true);
    });

    it("open signup modals", function() {
        expect(1).toEqual(1);
    });

    it("register unexisting user", function() {
        expect(1).toEqual(1);
    });

    it("close window", function() {
        expect(1).toEqual(1);
    });

    it("mock login out", function() {
        expect(1).toEqual(1);
    });

    it("show imogies", function() {
        expect(1).toEqual(1);
    });

});