describe("Dispatch Spec", function() {

    it("login successful send the msg", function() {
        expect(true).toBe(true);
    });

    it("unlogin fail to send the msg", function() {
        expect(1).toEqual(1);
    });

    it("send the correct format msg to group", function() {
        expect(1).toEqual(1);
    });

    it("send the uncorrect format msg, error happens", function() {
        expect(1).toEqual(1);
    });
    
});